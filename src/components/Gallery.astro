---
import type { LandingImage } from "../content/landing";

interface Props {
  images: LandingImage[];
}

const { images } = Astro.props;
---

<section class="section-pad gallery" aria-labelledby="gallery-title">
  <div class="container">
    <div class="section-heading section-heading--split section-heading--wide">
      <div>
        <p class="eyebrow">Визуальный обзор</p>
        <h2 id="gallery-title">Фото и планы по каждому объекту</h2>
      </div>
      <p>
        Галерея подписана по адресам, чтобы проще сопоставить фасады, входные группы, интерьерные виды и планировочные материалы.
      </p>
    </div>

    <div class="gallery__grid">
      {images.map((image, index) => (
        <figure class:list={["gallery-tile", index === 0 && "gallery-tile--wide", image.role === "plan" && "gallery-tile--contain"]}>
          <img src={image.src} alt={image.alt} width="760" height="520" loading="lazy" />
          {image.caption && <figcaption>{image.caption}</figcaption>}
        </figure>
      ))}
    </div>
  </div>
</section>
